syntax = "proto3";

package crypto;

service CryptoService {
    rpc StreamPrices (Empty) returns (stream PriceUpdate) {}
}

message Empty {}
message PriceUpdate {
    float value = 1;
    string timestamp = 2;
}